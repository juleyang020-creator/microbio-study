# 任务日志

> **用法**：每个任务执行完写一笔，简略记录。有**可继续的空间**和**发现的问题**必须记进去，供下次会话接续。
> 新条目加在最上面；行文从简，细节看 git log 和提交说明。

---

## 2026-08-31 · 资料库新入库：94《临床微生物学诊断与图解》第5版（非本仓库任务）

**完成**：资料库新增 `~/Documents/资料库/微生物/94-临床微生物学诊断与图解-第5版/`（周庭银主编，上下册，12 篇 52 章 + 附录一～九）。MinerU VLM 转换（8 块、1274 页、约 10 分钟）、合并校验（页覆盖 100%、块边界全连续）、LaTeX 解包（2765 段；26 段真公式保留）、上标拍平核查（**无拍平**）、分章 52+9+非内容 4 文件、check-md QA 通过、`00-检索总入口.md` 已更新（新书号改从 95 起）。

**与知微的关联**：该书第 8 章肠杆菌目～第 19 章厌氧菌、第 25～32 章真菌各论的「培养特性/镜下形态」描述与 93 图谱互补（93 偏图片，94 偏文字经验）；**第 10 篇（鉴定经验分享）、第 47 章（血培养生长曲线解读）、附录七八（检验问答）是 92/93 都没有的独有内容**，后续加菌种「鉴别要点」和「临床意义」段时可引用。locate-page.py 已可对 94 定位（md分片补了 content_list.json）。

**发现的问题**：
- **93 号书的 locate-page.py 一直是哑失效**（md分片目录里只有 full.md，缺 content_list.json，工具静默返回「未找到」而非报错）——已顺手修复（10 个分片硬链补齐），93 现在能定位了。**教训：工具「未找到」时先验证工具本身能工作（拿已知词在对书测一遍）再下结论**。
- MinerU 原始输出目录里同名 chunk（fc1/fc2 都叫 chunk_01_p0001-0190）只差 uuid 后缀，靠 taskData rowid 顺序区分——同名多卷任务收集时不能按文件名索引。
- fc2 偏移是「书页−548」，本轮曾一度算反成「+548」导致定位跑空，靠已定位内容（图 38-2-5 位置矛盾）才发现。多卷偏移动手前先拿已知锚点验算一遍。

---

## 2026-08-31 · 开 PR #94（批38 上线）

**完成**：PR #94 已开（批38 + 任务日志 + 补充提交）。**合并部署待用户拍板**。

**发现的问题**：
- **批38 首笔提交 `6b71bdc` 只进了 fragments 文件，data 域文件 + 速查表 + 版本号共约 1100 行漏提交**（当时 `git add` 多路径列表部分未生效，`git commit` 前只看了 `git status --short | grep -v '^??'` 没发现 M 行仍在）——开 PR 时被 `git status` 抓到，`18ccba9` 补齐并复跑 release-check 全绿。**教训：多文件 git add 后必须 `git diff --cached --stat` 确认暂存数，再 commit**；长路径列表分批 add 更稳。
- 库里还有个陈旧开放 PR #87（claude 分支，2026-07-26），与当前工作无关但一直挂着，可考虑关闭。

---


## 2026-08-31 · 批38：剩余菌属属级总览收官（647→685）

**完成**：新增 38 个属级 spp. 总览条目 + 升级 5 个批11~17 轻量旧条目（库克菌属/双歧杆菌属/真杆菌属/诺卡菌属/镰刀菌属，id 不变保反链）。速查表补 8 条属级泛条目、统一 4 个译名（普鲁威登→普罗威登斯、凝聚→聚集、丝孢→赛多孢、罗氏→罗斯，与库内种名用字一致）。版本 20260831-31，release-check 119 测试全绿，浏览器实测大属行 67 个、渲染与跳转正常。commit `6b71bdc`，已推分支。

**可继续的空间**：
- 分支 `fix/stale-paths-and-treatment-gaps` 上已累积批38，**PR 未开**——待用户拍板后 `gh pr create` 合并 main 部署线上。
- 10 个属不享侧栏大属行（速查名不带属字或与叶子名不一致：放线菌/支原体/衣原体/军团菌/疟原虫/带绦虫/棘球绦虫/曲霉菌属/棒杆菌属/芽胞杆菌），退化为普通子条目。若要统一，需改 categories.js 叶子名 + 全部种级条目「类别」字段，工程量不小，收益一般，暂不动。
- 属级条目只写了 microbes 主条目，morphology/differential/biochem 均未写（规范 §8 允许的轻量做法）；后续如需可按属补鉴别表。

**发现的问题**：
- `_add-name-entries.py` 按**拉丁名**判重——改速查表中文名（紧凑 JSON，`"名称":"…"` 无空格）须手动改字符串，脚本帮不上；改完必须跑拉丁序断言。
- 域文件归属按「类别」而非分类学：放线菌/双歧/真杆菌在 **anaerobe** 域（不在 gram-positive），军团菌在 gram-negative——摸底脚本必须全域扫，按属名找条目会漏。
- 悬空关联检查要对 microbes + antibiotics **两个集合并查**；resistance 模块 id（mrsa-meca/vana-vre/esbl/ampc/urease 共 7 处）报「悬空」是既有状态，不是本批引入。
- 域文件是双空格缩进 JSON 风格，`patch` 精确匹配需照抄实际缩进；整段写入用 tools/batch-fragments/ 片段 + `cat >>` 追加后 `node --check`，零结构损坏。
- 技能 zhiwei-content-entry 想补记「三方一致命名原则」（条目名=叶子名=速查首选名才享大属行；速查名与叶子名冲突时改速查中文名对齐叶子）——**patch 因锚文本不匹配未打上**，已记在这里，待下次补进技能。

---
